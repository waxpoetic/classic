.row
  .large-12.columns
    h1 artist roster
    if controller
      ul data-orbit=''
        each controller
          li
            img src=cover_photo alt=name
            .orbit-caption = name
    else
      p no artists to display
